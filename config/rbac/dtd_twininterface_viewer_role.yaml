# This rule is not used by the project twinscale-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to dtd.core.twinscale.io resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: twinscale-operator
    app.kubernetes.io/managed-by: kustomize
  name: dtd-twininterface-viewer-role
rules:
- apiGroups:
  - dtd.core.twinscale.io
  resources:
  - twininterfaces
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - dtd.core.twinscale.io
  resources:
  - twininterfaces/status
  verbs:
  - get
