kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  extraPortMappings:
  - containerPort: 30072    # the NodePort you chose
    hostPort: 30072         # published to your VM
    protocol: TCP
- role: worker
- role: worker
- role: worker
